on: workflow_dispatch
name: 🚀 Build & Upload
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: Use Node.js 14
      uses: actions/setup-node@v2
      with:
        node-version: '14'
      
    - name: 🔨 Build Project
      run: |
        npm install
        npm run build
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.2
      with:
        server: ftp5.webzdarma.cz
        username: ghactionstest.wz.cz
        password: GhActionsTest1
        local-dir: ./dist/
        server-dir: /web/dist/
